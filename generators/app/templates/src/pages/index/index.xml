<view class="{{styles.container}}">
  <view>
    <input placeholder="粤B-12345" auto-focus/>
  </view>
  <view  bindtap="bindViewTap" class="{{styles.userinfo}}">
    <image class="{{styles.avatar}}" src="{{userInfo.avatarUrl}}" background-size="cover"></image>
    <text class="{{styles.nickname}}">{{userInfo.nickName}}</text>
  </view>
  <view class="usermotto">
    <text class="user-motto">{{motto}}</text>
  </view>
</view>
